package org.tbwork.anole.hub.server.receive;

/**
 * AnoleReceiveServer is used for anole publisher client.
 * An AnoleReceiveServer receives writing requests of configuration.
 * But the primary difference between the AnoleReceiveServer and
 * the AnolePushServer is that the AnoleReceiveServer uses ordinary
 * TCP connection (also known as "short connection"), while the
 * AnolePushServer uses long TCP connection.
 * @author Tommy.Tang
 */
public class AnoleReceiveServer {

	
	
	
}
