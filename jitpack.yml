jdk:
  - openjdk11

install:
  - mvn clean install -DskipTests=true

artifacts:
  paths:
    - anole-loader/target/*.jar
    - spring-anole-loader/target/*.jar
    - anole-remote-apollo/target/*.jar
    - anole-log4j2/target/*.jar